interface dataProps{
    text:{
        title:string,
        images:string[]
    }
}

const EditThread = ({data}:dataProps) => {
  return (
    <div>
      
    </div>
  )
}

export default EditThread
